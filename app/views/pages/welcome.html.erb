<header style="padding-top: 0px;" class="jumbotron masthead">
  <div class="inner">
    <h1 style="font-size: 50px;">Watch over your network.</h1>    
    <p>See when your friends are feeling down.

<center>
<a style="margin-top: 30px; width: 400px; font-size: 40px; display: block;" class="btn btn-large btn-success" data-toggle="modal" href="#myModal" >Get started</a>
</center>
    </p>

  </div>
</header>

<p>

  
  


</p>







<div class="modal hide" id="myModal">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3>Connect Twitter & Linkedin</h3>
  </div>
  <div class="modal-body">
     <% %w[facebook twitter].each do |service| %>
  <ul class="services">
    <li class="service-name">
      <%= service.capitalize %>
      <% if @profiles && @profiles[service] %>
        <div class="service-is-connected">
          is connected
          <% @profiles[service] %>
        </div>
      <% else %>
        <div class="service-not-connected">
        <%= link_to "Connect", "/auth/singly?service=#{service}" %>
      </div>
      <% end %>
    </li>
  </ul>
  <% end %>
  </div>
  <div class="modal-footer">
    <a href="#" class="btn" data-dismiss="modal">Close</a>
    <a href="#" class="btn btn-primary">Save changes</a>
  </div>
</div>
